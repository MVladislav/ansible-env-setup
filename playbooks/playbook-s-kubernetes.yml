---
# playbook kubernetes

# ------------------------------------------------------------------------------
# ------------------------------------------------------------------------------
- name: Kubernetes installer
  remote_user: "{{ ansible_user }}"
  hosts:
    - kubernetes
  roles:
    ### -------------------------
    - role: ansible-kubernetes
    ### -------------------------
